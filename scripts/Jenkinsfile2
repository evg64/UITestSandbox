pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'whoami'
                sh 'printenv'
                sh 'adb devices'
            }
        }
    }
}